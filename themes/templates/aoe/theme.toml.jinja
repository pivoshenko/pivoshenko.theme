appearance = "dark"

background = "{{ role.bg.canvas.hex }}"
border = "{{ role.border.default.hex }}"
terminal_border = "{{ role.accent.primary.hex }}"
selection = "{{ role.bg.raised.hex }}"
session_selection = "{{ role.bg.overlay.hex }}"
title = "{{ role.accent.primary.hex }}"
text = "{{ role.fg.default.hex }}"
dimmed = "{{ role.fg.faint.hex }}"
hint = "{{ role.fg.muted.hex }}"
running = "{{ role.accent.success.hex }}"
waiting = "{{ role.accent.warning.hex }}"
fresh_idle = "{{ role.accent.primary.hex }}"
idle = "{{ role.fg.faint.hex }}"
error = "{{ role.accent.danger.hex }}"
terminal_active = "{{ role.accent.primary.hex }}"
group = "{{ role.fg.muted.hex }}"
search = "{{ role.accent.warning.hex }}"
accent = "{{ role.accent.primary.hex }}"
diff_add = "{{ role.accent.success.hex }}"
diff_delete = "{{ role.accent.danger.hex }}"
diff_modified = "{{ role.accent.warning.hex }}"
diff_header = "{{ role.accent.info.hex }}"
help_key = "{{ role.accent.primary.hex }}"
branch = "{{ role.accent.info.hex }}"
sandbox = "{{ role.accent.secondary.hex }}"

[syntax]
shiki_theme = "github-dark"
