[theme]
name = "catppuccin"

[theme.custom]
accent = "{{ role.accent.primary.hex }}"
panel_bg = "{{ role.bg.canvas.hex }}"
sidebar_bg = "{{ role.bg.sunken.hex }}"
active_row_bg = "{{ role.bg.canvas.hex }}"
selection_bg = "{{ role.bg.surface.hex }}"
surface0 = "{{ role.bg.surface.hex }}"
surface1 = "{{ role.bg.raised.hex }}"
surface_dim = "{{ role.bg.sunken.hex }}"
overlay0 = "{{ overlay0.hex }}"
overlay1 = "{{ overlay1.hex }}"
text = "{{ role.fg.default.hex }}"
subtext0 = "{{ role.fg.subtle.hex }}"
mauve = "{{ mauve.hex }}"
green = "{{ green.hex }}"
yellow = "{{ yellow.hex }}"
red = "{{ red.hex }}"
blue = "{{ blue.hex }}"
teal = "{{ teal.hex }}"
peach = "{{ peach.hex }}"
