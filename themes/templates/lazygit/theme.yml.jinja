theme:
  activeBorderColor:
    - "{{ role.accent.info.hex }}"
    - bold
  inactiveBorderColor:
    - "{{ role.fg.subtle.hex }}"
  optionsTextColor:
    - "{{ role.accent.info.hex }}"
  selectedLineBgColor:
    - "{{ role.bg.surface.hex }}"
  cherryPickedCommitBgColor:
    - "{{ role.bg.raised.hex }}"
  cherryPickedCommitFgColor:
    - "{{ role.accent.info.hex }}"
  unstagedChangesColor:
    - "{{ role.accent.danger.hex }}"
  defaultFgColor:
    - "{{ role.fg.default.hex }}"
  searchingActiveBorderColor:
    - "{{ role.accent.warning.hex }}"

authorColors:
  "*": "{{ lavender.hex }}"
