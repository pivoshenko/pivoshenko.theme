[{{ name }}]
text = {{ role.fg.default.hex | replace('#', '') }}
subtext = {{ role.fg.default.hex | replace('#', '') }}
main = {{ role.bg.canvas.hex | replace('#', '') }}
main-elevated = {{ role.bg.surface.hex | replace('#', '') }}
highlight = {{ surface0 | mix(color=surface1, amount=0.35) | get(key='hex') | replace('#', '') }}
highlight-elevated = {{ role.bg.raised.hex | replace('#', '') }}
sidebar = {{ role.bg.sunken.hex | replace('#', '') }}
player = {{ crust.hex | replace('#', '') }}
card = {{ role.bg.surface.hex | replace('#', '') }}
shadow = {{ role.bg.sunken.hex | replace('#', '') }}
selected-row = {{ overlay2.hex | replace('#', '') }}
button = {{ overlay1.hex | replace('#', '') }}
button-active = {{ overlay2.hex | replace('#', '') }}
button-disabled = {{ overlay0.hex | replace('#', '') }}
tab-active = {{ role.bg.surface.hex | replace('#', '') }}
notification = {{ role.bg.surface.hex | replace('#', '') }}
notification-error = {{ role.accent.danger.hex | replace('#', '') }}
equalizer = 000000
misc = {{ role.bg.raised.hex | replace('#', '') }}

crust = {{ crust.hex | replace('#', '') }}
mantle = {{ mantle.hex | replace('#', '') }}
base = {{ base.hex | replace('#', '') }}
surface0 = {{ surface0.hex | replace('#', '') }}
surface1 = {{ surface1.hex | replace('#', '') }}
surface2 = {{ surface2.hex | replace('#', '') }}
overlay0 = {{ overlay0.hex | replace('#', '') }}
overlay1 = {{ overlay1.hex | replace('#', '') }}
overlay2 = {{ overlay2.hex | replace('#', '') }}
rosewater = {{ rosewater.hex | replace('#', '') }}
flamingo = {{ flamingo.hex | replace('#', '') }}
pink = {{ pink.hex | replace('#', '') }}
maroon = {{ maroon.hex | replace('#', '') }}
red = {{ red.hex | replace('#', '') }}
peach = {{ peach.hex | replace('#', '') }}
yellow = {{ yellow.hex | replace('#', '') }}
green = {{ green.hex | replace('#', '') }}
teal = {{ teal.hex | replace('#', '') }}
sapphire = {{ sapphire.hex | replace('#', '') }}
blue = {{ blue.hex | replace('#', '') }}
sky = {{ sky.hex | replace('#', '') }}
mauve = {{ mauve.hex | replace('#', '') }}
lavender = {{ lavender.hex | replace('#', '') }}
